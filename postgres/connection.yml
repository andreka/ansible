---
  - name: Set  postgresql.conf
    hosts: all
    tasks:
    - name: Set maximum connections in postgresql.conf
      community.postgresql.postgresql_set:
        name: max_connections
        value: "200"
