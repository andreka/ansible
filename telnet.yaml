---
  - name: "Testing Ports"
    hosts: all
    vars:
      ports: 
        - 22
        - 80
        - 443
    tasks:
    - name: "Test Connection Telnet"
      shell: "telnet 192.168.59.133 {{ item }}"
      debug: msg
      loop: "{{ ports }}"

   
